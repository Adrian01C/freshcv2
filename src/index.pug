doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Virgil Ciociu - CV
    link(rel="stylesheet" href="styles.css")
    link(rel="stylesheet" href="css/style.css")
    script(src="https://code.jquery.com/jquery-3.6.0.min.js")
  body
    header
      h1.blue Virgil A Ciociu
      h2.teal-blue IT Desk Support Specialist
    section
      h3 About Me
      p As a dedicated and detail-oriented IT Desk Support Specialist...
      button.button.button-default Download my CV
    section
      h3 Let's Connect
    section
      h3 Skills
      ul
        li.blue HTML5
        li.teal-blue JavaScript
        <!-- More skills here -->
    script.
      $(document).ready(function() {
        $.get("https://ipinfo.io?token=aa50182e68a470", function(response) {
          var country = response.country;
          console.log("Țara utilizatorului: " + country);
          if (country === "RO") {
            changeLanguageTo("ro");
          } else if (country === "FR") {
